services:
  resume:
    build: .
    ports:
      - "3001:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/blog.db
      - IP_HASH_SALT=${IP_HASH_SALT:-change-me-set-in-env}
    volumes:
      - resume-data:/app/data

volumes:
  resume-data:
